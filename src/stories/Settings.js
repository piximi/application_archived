import React from 'react';
import { storiesOf } from '@storybook/react';
import Settings from '../components/Dialog/Settings/Settings';

storiesOf('Settings', module).add('default', () => <Settings />);
