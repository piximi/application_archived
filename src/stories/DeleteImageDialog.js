import React from 'react';
import { storiesOf } from '@storybook/react';
import DeleteImageDialog from '../components/DeleteImageDialog/DeleteImageDialog';

storiesOf('DeleteImageDialog', module).add('default', () => (
  <DeleteImageDialog />
));
