import React from 'react';
import { storiesOf } from '@storybook/react';
import DeleteButton from '../components/DeleteButton/DeleteButton';

storiesOf('DeleteButton', module).add('default', () => <DeleteButton />);
