import React from 'react';
import { storiesOf } from '@storybook/react';
import DeleteButton from '../components/AppBar/DeleteButton/DeleteButton';

storiesOf('DeleteButton', module).add('default', () => <DeleteButton />);
