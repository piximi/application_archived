import React from 'react';
import { storiesOf } from '@storybook/react';
import SettingsDialogTabContainer from '../components/SettingsDialogTabContainer';

storiesOf('SettingsDialogTabContainer', module).add('example', () => (
  <SettingsDialogTabContainer />
));
