import React from 'react';
import { storiesOf } from '@storybook/react';
import SaveDialog from '../components/SaveDialog/SaveDialog';

storiesOf('SaveDialog', module).add('default', () => <SaveDialog />);
