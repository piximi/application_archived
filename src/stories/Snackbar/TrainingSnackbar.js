import React from 'react';
import { storiesOf } from '@storybook/react';
import TrainingSnackbar from '../components/Snackbar/TrainingSnackbar/TrainingSnackbar';

storiesOf('TrainingSnackbar', module).add('default', () => (
  <TrainingSnackbar />
));
