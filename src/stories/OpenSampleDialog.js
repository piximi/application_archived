import React from 'react';
import { storiesOf } from '@storybook/react';
import OpenSampleDialog from '../components/Dialog/OpenSampleDialog/OpenSampleDialog';

storiesOf('OpenSampleDialog', module).add('default', () => (
  <OpenSampleDialog />
));
