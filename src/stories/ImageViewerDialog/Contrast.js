import React from 'react';
import { storiesOf } from '@storybook/react';
import Contrast from '../../components/Dialog/ImageViewerDialog/Contrast/Contrast';

storiesOf('Contrast', module).add('default', () => <Contrast />);
