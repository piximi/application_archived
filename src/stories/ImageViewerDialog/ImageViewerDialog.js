import React from 'react';
import { storiesOf } from '@storybook/react';
import ImageViewerDialog from '../../pages/image/ImageViewerDialog/ImageViewerDialog';

storiesOf('ImageViewerDialog', module).add('default', () => (
  <ImageViewerDialog />
));
