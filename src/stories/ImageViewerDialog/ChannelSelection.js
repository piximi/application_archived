import React from 'react';
import { storiesOf } from '@storybook/react';
import ChannelSelection from '../../components/ImageViewerDialog/ChannelSelection/ChannelSelection';

storiesOf('ChannelSelection', module).add('default', () => (
  <ChannelSelection />
));
