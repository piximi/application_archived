import React from 'react';
import { storiesOf } from '@storybook/react';
import ChannelSelection from '../../pages/image/ChannelSelection/ChannelSelection';

storiesOf('ChannelSelection', module).add('default', () => (
  <ChannelSelection />
));
