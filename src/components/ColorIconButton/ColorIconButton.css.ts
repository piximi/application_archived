import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    iconButton: {
      marginRight: '15px'
    },
    avatarRoot: {},
    colorPicker: {}
  });

export default styles;
