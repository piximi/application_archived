import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    iconButton: {
      margin: '0 15px 0 5px',
      padding: '3px'
    },
    avatarRoot: {},
    colorPicker: {
      margin: '16px'
    }
  });

export default styles;
