import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    button: {
      color: '#1a73e8',
      textTransform: 'none',
      fontSize: '.875rem',
      fontWeight: 500
    }
  });

export default styles;
