import {createStyles} from "@material-ui/styles";

const styles = () => createStyles({
  colorPicker: {
    height: '100%',
    padding: '12px'
  },
  iconButton: {
    padding: 0
  }
});

export default styles;
