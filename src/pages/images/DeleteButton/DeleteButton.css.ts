import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    button: {
      padding: '8px'
    },
    icon: {
      padding: '4px 8px'
    }
  });

export default styles;
