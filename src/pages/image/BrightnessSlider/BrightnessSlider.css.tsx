import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    root: {
      padding: '24px 24px 0 24px',
      width: 'calc(100% - 48px)'
    },
    slider: {
      padding: '22px 0px',
      color: 'white'
    }
  });

export default styles;
